# 任意设计图还原为 Pencil 设计稿：通用工作流

这是一份供 Codex 执行的通用操作手册。

输入可以是任意视觉设计图，例如：

- 手机 App 或小程序
- 网站、落地页、电商页
- 后台、数据看板、桌面应用
- 游戏 UI、活动界面、HUD
- 海报、信息图、品牌视觉稿
- 一张完整页面、局部组件或多页面拼图

输出是结构清晰、可编辑、可测量、可交付开发的 Pencil `.pen` 设计稿。

本流程不预设画面一定是手机、不预设固定尺寸，也不预设必须逐块等待用户确认。Codex 应先识别输入类型和用户目标，再选择合适的执行节奏。

---

## 1. 交付目标

高质量还原应同时满足以下要求：

### 1.1 视觉一致

- 画板比例、内容边界和整体构图接近原图。
- 颜色、字体层级、间距、圆角、边框和阴影关系正确。
- 图片裁切、叠层、透明度和遮挡符合原图。
- 重要视觉焦点、留白和信息密度没有被擅自改变。

### 1.2 结构可编辑

- 文本仍然是文本节点。
- 常规按钮、标签、卡片、列表和导航由 Pencil 节点构成。
- 重复元素尽可能组件化或保持一致结构。
- 复杂美术才使用独立图片素材。
- 不使用整张原图覆盖目标画板来冒充还原结果。

### 1.3 可供开发使用

- 主要图层具有语义化名称。
- 尺寸、位置、间距和层级可以被读取。
- 图片素材路径稳定、命名清楚。
- 可复用区域和状态差异能够被识别。
- 没有意外溢出、折叠或错误裁切。

---

## 2. 通用主流程

任何设计图都按下面八个阶段执行：

```text
理解任务
  ↓
识别画板与设计类型
  ↓
视觉审计与结构拆分
  ↓
建立坐标和尺寸基准
  ↓
制定 Pencil 实现策略
  ↓
先结构、后内容、再素材
  ↓
分层视觉与布局 QA
  ↓
整理交付
```

不得跳过“识别和拆分”直接开始批量绘制或生成图片。

---

## 3. 阶段一：理解任务

### 3.1 确认输入

开始前确认：

- 原始设计图的绝对路径。
- 目标是新建 `.pen`，还是修改现有 `.pen`。
- 还原整张图、某个画板，还是某个局部组件。
- 用户需要视觉稿、开发稿、组件库，还是三者兼有。
- 原图中是否包含多个页面、设备外框、说明区或品牌规范区。
- 项目内是否已有可复用素材。

### 3.2 判断执行节奏

根据用户表达选择：

- **逐步确认**：每完成一个可独立验收的区域就停下来让用户检查。
- **一次完成**：内部仍按阶段执行，但只在关键节点汇报，完成后统一交付。
- **只做局部**：严格限制在用户指定的区域，不顺便改其他部分。

如果用户没有指定，复杂页面默认先给出简短模块规划，再开始实施。

### 3.3 明确不能擅自做的事

- 不擅自改变设计风格。
- 不因为“更现代”而重设计原稿。
- 不在未确认前生成大量无关素材。
- 不把低清原图直接放大当作开发素材。
- 不读取或直接修改 `.pen` 的文件内容。

---

## 4. 阶段二：识别画板与设计类型

不要看到一张长图就默认它是手机页面。先识别：

### 4.1 画板边界

区分：

- 真正需要还原的画面
- 设备外框或浏览器外壳
- 标注区
- 色板、Logo 方案或组件展示区
- 多页面拼接区
- 纯背景装饰

一张图中如果有多个独立页面，应先建立页面清单，分别还原为独立顶层画板。

### 4.2 设计类型

根据视觉特征分类：

| 类型 | 常见特征 |
|---|---|
| 手机/小程序 | 窄竖屏、状态栏、底部导航、触控组件 |
| 响应式网页 | 顶部导航、多栏内容、页面长滚动 |
| 后台/看板 | 侧栏、工具栏、表格、筛选器、图表 |
| 游戏 UI | 强装饰框、复杂美术、HUD、弹窗、资源栏 |
| 海报/品牌稿 | 强排版、自由叠层、非交互构图 |
| 组件或局部截图 | 单一控件、弹窗、卡片或状态集合 |

分类的作用是选择合适的 Pencil guideline、布局方式和 QA 标准，不是限制设计。

### 4.3 是否需要设备外框

- 如果设备外框属于展示稿的一部分，则单独建为外层画板。
- 如果用户只要可开发页面，则设备外框通常作为参考，不进入实际页面组件。
- 不确定时，先说明两者差异，再根据用户目标选择。

---

## 5. 阶段三：视觉审计与结构拆分

### 5.1 从外到内建立层级

按下面顺序观察：

1. 画布背景
2. 顶层画板或窗口
3. 全局导航、状态栏、侧栏
4. 主要内容区块
5. 重复组件
6. 局部文字和图标
7. 覆盖层、悬浮层、装饰层

先形成结构树，再开始绘制。

示例结构树：

```text
Page
├── Global navigation
├── Main content
│   ├── Hero
│   ├── Section
│   │   ├── Heading
│   │   └── Repeated cards
│   └── Footer
└── Floating action
```

具体名称随设计变化，不照搬示例。

### 5.2 区分结构和美术

每个视觉元素都要归入以下一种：

| 类别 | 推荐实现 |
|---|---|
| 文本 | Pencil Text |
| 简单形状、边框、分割线 | Pencil Frame/Shape |
| 常规图标 | Pencil 图标库或简单 Path |
| 规则按钮、标签、输入框 | Pencil 可编辑组件 |
| 照片、插画、纹理 | 图片 Fill |
| Logo、复杂装饰、角色、手绘图 | 独立图片素材 |
| 重复列表项 | 可复用组件或统一复制结构 |
| 图表、表格 | Pencil 布局节点，不整图截图 |

### 5.3 识别重复模式

寻找：

- 同类卡片
- 相同按钮
- 标签或状态
- 导航项
- 表格行
- 用户信息块
- 多个尺寸或状态变体

先做一个正确母版，再复制。不要手工画多个略有差异但本质相同的结构。

### 5.4 提取视觉规则

至少记录：

- 主背景色与内容背景色
- 主要文字色、次要文字色
- 品牌色和强调色
- 字体家族、字号层级、字重
- 常用圆角
- 常用描边
- 主要间距单位
- 阴影和模糊的使用位置

这些规则可以成为 Pencil variables；不必为只出现一次的值强行创建变量。

---

## 6. 阶段四：建立坐标和尺寸基准

### 6.1 原图坐标基准

记录：

- 原图像素尺寸
- 目标画板在原图中的矩形
- 目标 Pencil 画板尺寸

如果目标画板与原图区域同比缩放，可以使用：

```text
scaleX = Pencil 画板宽 / 原图目标区域宽
scaleY = Pencil 画板高 / 原图目标区域高

Pencil X = (元素原图 X - 目标区域左边界) × scaleX
Pencil Y = (元素原图 Y - 目标区域上边界) × scaleY
Pencil W = 元素原图宽 × scaleX
Pencil H = 元素原图高 × scaleY
```

如果 `scaleX` 与 `scaleY` 差异明显，先检查是不是选错了目标区域，不能直接把画面压扁。

### 6.2 测量优先级

优先测量对全局影响最大的尺寸：

1. 画板宽高
2. 全局边距
3. 顶部、侧边和底部固定区域
4. 主内容列宽
5. 大区块高度
6. 卡片和重复项
7. 内部文字和图标

不要从一个小图标开始测量。

### 6.3 规律优先于逐像素猜测

如果多个元素显然遵循相同网格，应先找出：

- 相同左右边距
- 相同列宽
- 相同卡片间距
- 相同组件高度
- 4、8、10、12、16、20、24 等重复间距

用一致的规则还原，通常比对每个模糊边缘分别猜像素更可靠。

### 6.4 OCR 和视觉测量的使用边界

OCR 不是默认步骤。

适合使用 OCR 或视觉测量工具的情况：

- 大量文本需要识别。
- 字号、行距或文本框边界无法肉眼判断。
- 需要对比原图和还原稿的文字偏差。
- 用户要求高精度量化报告。

不适合：

- 文本很少且清晰可读。
- 当前只在搭框架。
- 用户要求快速逐块绘制。

---

## 7. 阶段五：制定 Pencil 实现策略

在开始批量编辑前，形成一份简短策略：

### 7.1 顶层画板计划

- 参考图画板
- 目标还原画板
- 必要的状态或尺寸变体
- 可复用组件区
- 可选开发交付区

根文档保持干净。普通文本、按钮和图片不要直接散落在 document 根节点。

### 7.2 布局策略

- 规则的横向或纵向内容：使用 Pencil 布局。
- 精确叠层、自由海报、HUD 或覆盖层：使用 `layout: "none"`。
- 复杂页面可以混合：外层规则布局，局部自由定位。
- 不为追求“全自动布局”而破坏原图的精确构图。

### 7.3 组件策略

适合做成可复用组件：

- 出现三次以上且结构一致的元素
- 明确存在状态变体的控件
- 全局导航项
- 统一卡片
- 统一按钮或输入控件

不适合强行组件化：

- 只出现一次的装饰
- 内容结构差异很大的区块
- 纯视觉叠层海报元素

### 7.4 素材策略

在绘制前建立素材清单，并标记：

```text
已有可用
可以用 Pencil 绘制
可以从源文件获得
需要裁切
需要生成
暂时占位
```

只有确定需要生成的素材才进入生图流程。

---

## 8. Pencil MCP 操作规范

`.pen` 是加密设计文件，只能通过 Pencil MCP 读取和修改。

禁止：

- 使用 `cat`、`sed`、`rg` 等读取 `.pen` 内部。
- 用普通脚本直接修改 `.pen`。
- 猜测未读取的节点状态。

### 8.1 每次会话的起始动作

1. 调用 `get_editor_state(include_schema: true)`。
2. 确认当前激活文件。
3. 根据设计类型加载对应 guideline。
4. 用 `batch_get` 读取即将修改的节点。
5. 如果是现有文件，先确认用户是否已经手动修改。

### 8.2 根画板的 placeholder

修改顶层画板时：

```js
Update("rootFrameId", {placeholder: true})
```

完成当前批次后：

```js
Update("rootFrameId", {placeholder: false})
```

不要让完成画板长期保持 placeholder 状态。

### 8.3 节点命名

所有新增节点使用人类可读名称：

```text
Page Header
Primary Navigation
Hero Artwork
Product Card
Dialog Backdrop
Floating Action
```

名称描述功能或视觉角色，不使用无意义编号。

### 8.4 布局规则

- Frame 才能使用 layout 和 padding。
- 布局容器内优先使用 `fill_container`、`fit_content` 和 gap。
- 父容器是布局时，普通子节点的 `x/y` 不生效。
- 只有需要叠层或精确定位时使用 `layout: "none"`。
- 文本换行时正确设置 `textGrowth`。
- 不使用百分比尺寸或 CSS 表达式。

### 8.5 图片规则

Pencil 没有独立 `image` 节点；图片使用节点 Fill：

```js
Update("artworkNodeId", {
  fill: {
    type: "image",
    url: "./design/assets/example-v1.png",
    mode: "fill"
  }
})
```

模式选择：

- `fit`：完整显示，适合 Logo、图标、透明素材。
- `fill`：填满并裁切，适合照片、Banner、封面。
- `stretch`：可能变形，只在比例完全一致时使用。

### 8.6 复制复杂节点

复制带子节点的结构时，在同一次 `Copy` 中通过 `descendants` 修改内容：

```js
copy = Copy("sourceId", "parentId", {
  name: "Card · Variant",
  descendants: {
    "titleId": {content: "New title"},
    "imageId": {
      fill: {
        type: "image",
        url: "./design/assets/new-image.png",
        mode: "fill"
      }
    }
  }
})
```

不能复制后继续用原子节点 ID 更新副本。

### 8.7 图层顺序

同一父节点中，后面的节点通常显示在更上层。

建立明确层级：

```text
Background
Content
Sticky or fixed navigation
Backdrop
Dialog / floating panel
Foreground decoration
```

半透明效果必须放在真实下层内容上检查，单独截图透明节点不能证明透底效果正确。

### 8.8 裁切

使用 `clip: true` 前先判断裁切是否属于设计意图：

- 横向滚动内容被视口裁切
- 图片封面裁切
- 圆角容器裁切
- 页面底部内容超出可视区

这些可能是正确行为。不要只为了消除布局警告而扩大容器。

### 8.9 图片缓存

Pencil 可能缓存已经使用过的图片 URL。替换同名文件后仍显示旧图时：

1. 使用新版本文件名，如 `hero-v2.png`。
2. 更新 Fill URL。
3. 截图确认。

不要反复覆盖同一个文件名并假设画布一定刷新。

---

## 9. 阶段六：通用构建顺序

无论设计类型如何，推荐顺序如下。

### 9.1 建立参考

- 保留原设计图作为独立参考画板或外部参考。
- 参考和目标画板分开。
- 不在原图上直接覆盖可编辑节点作为最终交付。

### 9.2 建立目标画板

- 使用正确宽高。
- 设置背景、裁切和主要布局方式。
- 如果有多页面，为每个页面建立独立顶层画板。

### 9.3 搭建宏观结构

先完成：

- 全局区域
- 主内容列或网格
- 固定导航
- 大型面板
- 主要覆盖层

此阶段图片可以占位，文字使用真实或接近长度的内容。

### 9.4 设置视觉规则

- 颜色
- 字体
- 字号层级
- 圆角
- 描边
- 阴影
- 间距

先校准一套，再应用到重复元素。

### 9.5 完成一个代表性组件

例如第一个卡片、第一个表格行、第一个导航项或第一个商品项。

代表性组件确认正确后再复制，避免批量复制错误。

### 9.6 填充内容

- 替换真实文字。
- 设置正确换行。
- 放入图标。
- 调整重复项差异。
- 建立必要的状态。

### 9.7 替换美术素材

按素材清单逐个替换占位：

- 一次只处理一个素材家族。
- 替换后立刻截图检查比例和裁切。
- 确认 Pencil 使用的是最终文件，而不是旧缓存。

### 9.8 完成叠层和交互状态

最后处理：

- 弹窗
- 遮罩
- 悬浮按钮
- Sticky 区域
- Tooltip
- 选中、禁用、悬停等状态

这些元素依赖下层内容，过早制作不利于判断透明和层级。

---

## 10. 通用素材决策树

遇到图片区域时依次判断：

```text
项目是否已有可用素材？
├── 是 → 直接复用
└── 否
    ├── 用户是否提供了清晰素材？
    │   ├── 是 → 检查尺寸、透明边和裁切后使用
    │   └── 否
    ├── 是否可用 Pencil 简单绘制？
    │   ├── 是 → 保持可编辑
    │   └── 否
    ├── 原图素材是否足够清晰且能独立裁切？
    │   ├── 是 → 裁切、清理、检查
    │   └── 否
    └── 使用适合的素材生成技能
```

### 10.1 何时使用 `asset-gen`

适用于缺失或低清的静态素材：

- UI 图标
- Logo 或徽章重制
- Banner 独立美术
- 头像
- 道具
- 吉祥物
- 装饰件
- 透明背景角色或物体

使用前完整读取当前环境中的 `asset-gen/SKILL.md`。如果技能要求调用其他生图技能，也要完整读取其说明。

### 10.2 生成素材的通用流程

```text
从原图裁参考
  ↓
图生图或文生图
  ↓
使用高对比纯色背景
  ↓
转透明并 bbox 裁切
  ↓
肉眼检查边缘和完整性
  ↓
保存为稳定语义名称
  ↓
替换 Pencil 占位
```

如果主体被抠图背景污染，应换背景色重新生成，不要反复硬修脏边。

### 10.3 用户批准的素材

用户明确认可某张素材后应优先使用。只有存在明确的开发问题时才提出更换，并说明原因。

---

## 11. 不同设计类型的分支

主流程相同，下面只列出额外关注点。

### 11.1 手机与小程序

- 状态栏和安全区
- 顶部栏
- 可滚动内容
- 底部导航
- 悬浮操作
- 触控目标尺寸

### 11.2 网站与落地页

- 页面最大宽度
- 响应式列
- Header 和 Footer
- 首屏高度
- Section 间距
- 桌面与移动变体

### 11.3 后台与数据看板

- 侧栏、顶部工具栏
- 表格严格层级
- 筛选器
- 图表图例
- 数据密度
- 行列对齐

### 11.4 游戏 UI

- HUD 层级
- 美术框与可点击区域分离
- 九宫格拉伸区域
- 资源栏、技能栏、弹窗
- 静态素材和运行时特效的边界
- 不把运行时 VFX 当作普通静态 PNG 处理

### 11.5 海报与品牌稿

- 使用自由定位和叠层
- 重点检查字体、基线、视觉平衡
- 复杂文字特效可能需要图片素材
- 不强行套用 App 的自动布局逻辑

### 11.6 局部组件

- 只建立必要画板
- 保留所有状态
- 标注尺寸和变体
- 不扩展成完整页面，除非用户要求

---

## 12. 阶段七：QA 验收

QA 必须同时包含结构检查和视觉检查。

### 12.1 结构检查

使用 `snapshot_layout(problemsOnly: true)` 检查：

- 意外裁切
- 内容溢出
- 折叠
- 容器尺寸异常
- 图层超出画板

每个问题都要分类：

- 真错误：修复。
- 设计意图：保留并记录。

### 12.2 视觉检查

使用 `get_screenshot`：

- 局部修改截局部。
- 完成一个区域截区域。
- 最终完成截整个画板。

检查：

- 构图
- 对齐
- 间距
- 字体层级
- 颜色
- 图片裁切
- 圆角和边框
- 阴影与透明度
- 图层顺序
- 内容完整性

### 12.3 对比检查

需要高精度时：

1. 导出目标画板。
2. 将其与原图目标区域缩放到相同尺寸。
3. 做并排对比或透明叠加。
4. 优先修正大结构偏差，再修正局部细节。

不要先花时间修一个 1 px 图标偏差，而页面整体列宽仍然错误。

### 12.4 最终检查清单

- [ ] 画板数量和尺寸正确。
- [ ] 原图中的主要结构均已还原。
- [ ] 文本仍可编辑。
- [ ] 重复元素保持一致。
- [ ] 图片素材使用正确版本。
- [ ] `fit`、`fill` 和裁切选择正确。
- [ ] 没有明显拉伸或抠图脏边。
- [ ] 覆盖层与固定层顺序正确。
- [ ] 没有意外布局警告。
- [ ] 有意裁切没有被误修。
- [ ] 顶层画板不处于 placeholder 状态。
- [ ] 最终截图已检查。

---

## 13. 常见失败及处理

### 13.1 未分析就开始画

结果：画错画板、漏掉模块或把说明区当页面。

处理：先识别画板边界并列结构树。

### 13.2 一次生成大量素材

结果：素材风格、比例或用途与最终容器不匹配。

处理：先确定容器，再按素材家族逐个生成。

### 13.3 把容器问题误判为图片问题

结果：错误裁图或破坏用户认可素材。

处理：分别检查透明 bbox、容器尺寸和图片模式。

### 13.4 生成素材后画布仍显示旧图

结果：以为生成失败，实际是 Pencil 缓存。

处理：使用新的版本化文件名并更新 Fill URL。

### 13.5 为消除警告破坏设计

结果：横向滚动、封面裁切或底部露出内容被错误展开。

处理：区分设计意图和真实布局问题。

### 13.6 只看整个页面截图

结果：局部字体、边缘和图片裁切无法判断。

处理：组件、区域、整页三个尺度分别检查。

### 13.7 未确认图层顺序

结果：帖子盖住导航、遮罩盖错对象、透明层没有真实背景。

处理：建立明确的背景、内容、固定层、覆盖层顺序。

### 13.8 过度依赖 OCR

结果：在结构阶段浪费时间和环境成本。

处理：只在文字识别或精确文本测量确有必要时使用。

### 13.9 擅自优化设计

结果：做成了“更好看但不一样”的新设计。

处理：还原任务以原图为准；改版必须单独获得授权。

---

## 14. 与用户协作

工作中应持续说明：

- 当前正在处理哪个区域。
- 这一轮是否包含素材生成。
- 哪些部分暂时只是占位。
- 发现的关键差异和判断依据。

不要让用户无法判断 Codex 当前在做什么。

用户提出局部修改时：

1. 先确认问题属于素材、容器、样式还是层级。
2. 只修改必要范围。
3. 修改后截对应局部。
4. 等用户确认或继续下一项。

用户的明确批准高于 Codex 的主观审美偏好。

---

## 15. 最终交付

交付内容按任务需要包含：

- `.pen` 文件
- 最终图片素材
- 可选页面导出图
- 可选组件导出
- 仍然存在的设计不确定项

最终回复应简洁说明：

- 已完成什么
- 文件在哪里
- 是否通过结构和视觉检查
- 下一步是否需要用户选择

---

## 16. Codex 默认执行指令

以后遇到任意“根据设计图还原 Pencil”任务，执行：

```text
1. 完整读取本手册。
2. 检查输入图、目标文件和现有素材。
3. 识别设计类型、画板边界和交付目标。
4. 建立结构树、重复模式和素材清单。
5. 确定坐标、布局、组件和图层策略。
6. 先创建可编辑结构，再填内容和美术。
7. 缺失或低清静态素材按 asset-gen 流程处理。
8. 所有 .pen 操作只通过 Pencil MCP。
9. 每个完整阶段做局部截图和布局检查。
10. 最终做整页视觉 QA 并交付。
```

如果用户指定制作顺序、工具、素材或审批节奏，以用户要求为准，但不得跳过必要的文件安全和最终 QA。
