# Pencil Files

Save real Pencil `.pen` files here from the Pencil editor.

Recommended filename:

```text
cabakura-app.pen
```

Do not create `.pen` files with `touch`, `cat`, scripts, or text editors.

